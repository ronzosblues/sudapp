<%
nombres = @user.username.partition(" ")  
@nombre=""
nombres.each do |palabra|
@nombre = @nombre+palabra.capitalize
end
%>

<h4>SUDAMERICA SKATEBOARDING | CONTACTO</h4>

<p>Bienvenido <%= @nombre %>!</p>

<p>Confirma tu cuenta entrando en el siguinte link:</p>

<p><%= link_to 'Confirmar mi cuenta', confirmation_url(@resource, :confirmation_token => @token) %></p>
