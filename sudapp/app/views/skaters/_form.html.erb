<%= form_for(@skater) do |f| %>
  <% if @skater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skater.errors.count, "error") %> prohibited this skater from being saved:</h2>

      <ul>
      <% @skater.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nombre completo" %>
    <%= f.text_field :skater_name %>
  </div>
  <div class="field">
    <%= f.label "Ficha" %>
    <%= f.text_area :skater_body %>
  </div>
  <div class="field">
    <%= f.label "Foto" %>
    <%= f.file_field :skater_photo %>
  </div>
  <div class="actions">
    <%= f.submit "Enviar" %>
  </div>
<% end %>
