<% @album_id = params[:album_id].to_i %>
<%= form_for @photo, :html => { :multipart => true } do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Titulo" %>
    <%= f.text_field :photo_title%>
  </div>
 
  <div class="field">
    <%= f.label :album_id %>
    <%= f.collection_select :album_id, Album.all, :id, :album_title, {:selected => @album_id} %>
  </div>
<br>
    <div class="field">
    <%= f.file_field :photo, :class => 'input' %>
  </div>


  <div class="actions">
    <%= f.submit "Enviar"%>
  </div>
<% end %>