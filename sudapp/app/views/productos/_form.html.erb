<%= form_for(@producto) do |f| %>
  <% if @producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producto.errors.count, "error") %> prohibited this producto from being saved:</h2>

      <ul>
      <% @producto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_producto %><br>
    <%= f.text_field :nombre_producto %>
  </div>
  <div class="field">
    <%= f.label :descripcion_producto %><br>
    <%= f.text_area :descripcion_producto %>
  </div>
  <div class="field">
    <%= f.label :foto_producto %><br>
    <%= f.file_field :foto_producto %>
  </div>
  <div class="field">
    <%= f.label :tipo_producto %><br>
    <%= f.collection_select :tipo_producto_id, TipoProducto.all, :id, :nombre_tipo_producto %>
  </div>
  <div class="field">
    <%= f.label :precio %><br>
    <%= f.number_field :precio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
