<div class="secciones">S K A T E R S</div>
<p></p>
<%if current_user.try(:admin?) %>
  <%= link_to 'Crear un nuevo perfil', new_skater_path %>
<% end %>
</p></p>


    <% @skaters.each do |skater| %>
        <div class="my-div-bottom"><%= link_to skater.skater_name.upcase, skater, :class =>"fuente-tres"%></div>
        <%if current_user.try(:admin?) %>
        |<%= link_to ' Editar', edit_skater_path(skater) %> |
        <%= link_to ' Eliminar', skater, method: :delete, data: { confirm: 'Esta seguro que desea eliminar este perfil?' } %> |
        <%= link_to 'Agregar fotos al perfil', :controller => '/photo_skaters', :action => 'new', :skater_id => skater.id, :skater_name => skater.skater_name %> |
        <%= link_to 'Agregar video a la nota', :controller => '/video_skaters', :action => 'new', :skater_id => skater.id, :skater_name => skater.skater_name %> |
        <% end %>
        <p></p>
    <% end %>

<br>


