<%= form_for(@amigo) do |f| %>
  <% if @amigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amigo.errors.count, "error") %> prohibited this amigo from being saved:</h2>

      <ul>
      <% @amigo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Nombre' %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label 'Web' %>
    <%= f.text_field :web %>
  </div>
  <div class="actions">
    <%= f.submit 'enviar' %>
  </div>
<% end %>
